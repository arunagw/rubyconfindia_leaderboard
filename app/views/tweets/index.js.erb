$('#tweets').append('<%= j render(:partial => 'tweet',:locals => {:tweets => @tweets}) %>');
<% if @tweets.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@tweets) %>');
<% else %>
$('.pagination').remove();
<% end %>